# 42 — Project State

**Last updated:** 2026-09-15 · **Updated by:** Phase 7 implementation

This file is the single current-moment snapshot of where the project actually is. It is updated after every meaningful implementation milestone (per the PLAN → ... → UPDATE PROJECT STATE cycle in [40-ROADMAP.md](40-ROADMAP.md)) — if this file and a phase's task statuses in [41-TASKS.md](41-TASKS.md) ever disagree, treat that as a bug to fix immediately, not a stale-doc shrug.

## Current phase

**Phase 7 — Appointments: COMPLETE.** All tasks `T-701`–`T-711` done (see [41-TASKS.md](41-TASKS.md)). Next: **Phase 8 — Consultations / medical records** (`T-801` onward).

## Decision update: GitHub added for source control (2026-08-25)

Partway through Phase 2, the user reversed half of the original "no GitHub" decision ([ADR-011](43-ARCHITECTURE-DECISIONS.md), originally recorded during Stage 1 → Stage 2 transition): they created `github.com/wdpradip-dev/healthcare`, set a repo-local git identity (not global), and added it as the `origin` remote. GitHub is now used for source-control backup/history only — **the "no hosted CI/CD" half of the decision is unchanged**, no GitHub Actions workflow exists or is planned, and deploys remain the manual CLI-driven workflow in [32-DEPLOYMENT.md](32-DEPLOYMENT.md). ADR-011, `CLAUDE.md`, and the deployment docs were updated in the same change to stop claiming "no GitHub" outright.

## Completed features

**Phase 1 / Phase 2** — see prior entries in git history for detail: monorepo scaffold, full Prisma schema + initial migration, catalog/demo seed scripts, `SUPER_ADMIN` bootstrap script, soft-delete Prisma extension.

**Phase 3 — Backend (`apps/api`):**
- NestJS bootstrap: global guard chain (`RateLimitGuard` → `JwtAuthGuard` → `AuthorizationGuard`, registered via `APP_GUARD`) + `GlobalExceptionFilter` (`APP_FILTER`), all in `CommonModule`. Every controller method carries exactly one of `@Public()` / `@Authenticated()` / `@RequirePermission()`, enforced by a custom TypeScript-AST static checker (`scripts/check-route-permissions.mjs`) wired into `apps/api`'s `lint` script — not just a convention, a build-breaking check.
- `AuthModule`: register (OTP-gated activation) → verify-otp → login (lockout after 5 failed attempts, 15 min) → refresh (rotating opaque tokens, reuse-detection revokes the whole session family) → logout → forgot-password → reset-password (revokes all sessions) → sessions list/revoke → `me`. OTP: 6-digit, argon2-hashed, 5 min TTL, 5 max attempts, 30 s resend cooldown, 5/hour cap. No-enumeration on login and forgot-password (verified by test, not just by inspection).
- JWT RS256 access tokens (15 min) + SHA-256-hashed opaque refresh tokens (`packages/shared/src/auth/token-hash.ts`).
- `AuditInterceptor`/`AuditService` scaffold — `AuditLog` rows written on register/login/logout/password-reset today; wired for every future mutating route.
- `packages/validation/src/auth.ts` — the Zod schemas for every `/auth` DTO, shared verbatim between the API's `ZodValidationPipe` and both clients' `react-hook-form` resolvers (one schema, never duplicated). Fixed a real bug in this pass: `verifyOtpSchema`/`resetPasswordSchema`'s `code` field used `.length(6).regex(...)` — a code with the wrong length fell through to Zod's generic `"String must contain exactly 6 character(s)"` instead of the intended `"Code must be 6 digits."`, since `.length()` fires before `.regex()` and had no custom message. Fixed by giving `.length()` the same message.
- Verified via 16/16 passing integration tests (`apps/api/test/auth.integration-spec.ts`, Jest + Supertest against a full booted Nest app) covering register, verify-otp, login (incl. lockout), refresh (incl. reuse-detection), forgot/reset password (incl. no-enumeration), and `me` — plus ~17 unit tests across guards/pipes/JWT service/health controller.

**Phase 3 — Admin (`apps/admin`):** Login, Forgot Password, Reset Password pages (`src/app/{login,forgot-password,reset-password}`), all React Hook Form + `zodResolver` against the shared schemas, calling the API through Server Actions (`actions.ts` in each route folder) that never expose the refresh token to client JS.
- Session model matches docs/16-AUTHENTICATION.md exactly: refresh token lives in an **httpOnly, Secure (prod), SameSite=Strict cookie**, and — per `docs/33-ENVIRONMENT-VARIABLES.md`'s `SESSION_COOKIE_SECRET` ("signs/encrypts the admin's auth cookie wrapper") — the cookie value is sealed with `iron-session`'s `sealData`/`unsealData` (AES-GCM + signing), not stored as the raw token. The access token is never persisted client-side at all (no cookie, no `localStorage`); it lives only in a React Context (`src/lib/auth-provider.tsx`) re-derived on every app load by exchanging the sealed cookie via `POST /api/session/refresh`.
- New shared component primitives in `packages/ui/web` (`Button`, `TextField`, `FormAlert`, `AuthShell`) — the package was an empty Phase 1 placeholder before this.
- Verified two ways: 11 passing component tests (render / validation-error / API-error / success paths per all three screens, per docs/30-TESTING-STRATEGY.md's "Mobile & Admin coverage" minimum), **and** a real browser session against a running `next dev` server (client-side Zod validation, graceful API-unreachable error banner, forgot→reset navigation carrying the masked-destination/challenge-id query params, malformed-code/weak-password validation) — see "Known issues" for what that browser check could *not* cover.

**Phase 3 — Mobile (`apps/mobile`):** Splash, Welcome, Login, Register, OTP Verification, Forgot Password, Reset Password screens (`app/{index,welcome,login,register,otp-verify,forgot-password,reset-password}.tsx`), matching docs/08-MOBILE-DESIGN-MOCKUPS.md's "Authentication" wireframes screen-for-screen (including the two-step reset flow: OTP Verification confirms the code, Reset Password only asks for the new password — no code re-entry).
- Session model per docs/16: refresh token in Expo SecureStore (`src/lib/secure-storage.ts`); access token in memory only (`src/lib/auth-context.tsx`), re-derived on cold start via the same `/auth/refresh` → `/auth/me` sequence as admin.
- New shared component primitives in `packages/ui/native` (`Button`, `TextField`, `FormAlert`, `AuthScreen`, `OtpInput`) — also an empty Phase 1 placeholder before this.
- Scope decision: Login's "→ OTP Verification if unverified" branch (docs/08) is **not** implemented — resuming a lost `REGISTRATION` OTP challenge from just an identifier has no backing endpoint yet (only `forgot-password` can (re)issue a challenge, and only for `PASSWORD_RESET`). `AUTH_ACCOUNT_PENDING_ACTIVATION` is shown as a plain error message instead. A "resend registration OTP" endpoint is a small, clearly-scoped follow-up whenever this gap needs closing — not invented speculatively here per `CLAUDE.md`'s "don't invent new API routes without updating docs first."
- Verified via 18 passing component tests (React Native Testing Library) across all 6 screens. **A real bug was caught by this suite**: `OtpInput`'s auto-submit-on-6th-digit never fired, because its completion check was `!next.includes("")` on the *joined string* — every string trivially "includes" `""` in JavaScript, so that condition was always `false`. Fixed to rely on the already-sufficient `next.length === LENGTH` check. Not caught by typecheck/lint since it's a logic bug, not a type error — exactly the kind of thing this coverage requirement exists to catch.
- **Not verified in a simulator, Expo Go, or `expo start --web`** — see "Known issues".

**Four pre-existing infrastructure gaps, discovered (not introduced) while doing the above, and fixed in this pass:**
1. `apps/api/src/main.ts` never loaded `.env` for local dev (`pnpm --filter api dev`) — no `dotenv` import anywhere, so a real developer following `CLAUDE.md`'s documented `pnpm --filter api dev` command would hit "Invalid environment configuration" immediately on a fresh checkout. Fixed with a guarded `import "dotenv/config"` at the top of `main.ts` (no-ops if `.env` doesn't exist; never touches the test suite, which never imports `main.ts`).
2. `packages/config/src/schemas.ts`'s optional URL env vars (`SENTRY_DSN`, `NEXT_PUBLIC_SENTRY_DSN`, `EXPO_PUBLIC_SENTRY_DSN`, `OBJECT_STORAGE_ENDPOINT`, `RATE_LIMIT_REDIS_URL`, `API_INTERNAL_BASE_URL`) used `z.string().url().optional()` — but every `.env.example` sets these to `""` rather than omitting them, and `.optional()` doesn't help against an empty string. Copying `.env.example` to `.env` verbatim (the documented setup step) would hard-crash on startup. Fixed with a shared `optionalUrl` helper that treats `""` as unset before validating.
3. `apps/mobile/jest.config.js`'s `transformIgnorePatterns` — copied from Expo's standard template, which assumes a flat `node_modules/react-native/...` layout — never matched pnpm's nested `node_modules/.pnpm/<name>/node_modules/<name>/...` layout, so **no mobile test had ever successfully run** in this repo (`passWithNoTests: true` masked it since zero test files existed before this phase). Fixed with an optional `(\.pnpm/[^/]+/node_modules/)?` prefix; also added `maxWorkers: 1` (parallel jest-expo workers were exhausting available memory in this sandbox) and a `moduleNameMapper` for the `@/*` alias (needed once real imports existed to test against).
4. **`apps/api`'s `dev` script (`nest start --watch`, `tsc` builder) could not actually run at all.** It failed resolving `@hospital/database` (and any other workspace TS package): that package's `"main"` points at raw `./src/index.ts` — this monorepo consumes workspace packages as TS source everywhere (see `apps/admin`'s `transpilePackages` note), which only ever worked for `apps/api` in tests because `ts-jest` transpiles the whole dependency graph itself; plain `tsc`+`node` was never going to resolve a raw `.ts` "main". **Tried and reverted first:** `tsx watch src/main.ts` does fix the resolution problem (esbuild-based, confirmed working, NestJS DI included) but breaks `@nestjs/swagger`'s route-parameter introspection (`TypeError` in `ParameterMetadataAccessor.explore` — esbuild's decorator-metadata emission isn't a safe drop-in here). **Fixed properly** with `node -r ts-node/register/transpile-only --watch src/main.ts` — `ts-node` uses the real TypeScript compiler (not esbuild), so `emitDecoratorMetadata` stays correct while still transpiling workspace `.ts` on the fly. Verified end-to-end against the pglite stand-in: clean boot, `/health` and `/health/ready` (real DB check) both respond, `/api/v1/docs-json` (the thing that broke under `tsx`) responds correctly, a real `/auth/login` against seeded demo data succeeds and issues a working JWT, and `--watch` correctly detects a source-file edit and restarts the process (confirmed twice). `build`/`start` (`nest build` + `node dist/main.js`) share the identical root cause and remain unfixed — out of scope for this pass (which was scoped to the dev server specifically) but a real gap before Render deployment (Phase 15); the same `ts-node`-based approach, or building workspace packages to real JS with `"main"` pointing at `dist/`, are the two realistic fixes.

**Phase 4 — Backend (`apps/api`):** `HospitalsModule` (Super Admin only, `PLATFORM` scope, no tenant filter by definition), `BranchesModule`, `DepartmentsModule`, `UsersModule` (invite → activate → deactivate lifecycle) — four new domain modules, all wired into `AppModule`.
- **Tenant scoping**, unified across all three hospital-scoped modules via one helper, `apps/api/src/common/tenant-scope.util.ts`'s `resolveHospitalId(actor, suppliedHospitalId)`: an Admin's own JWT-embedded `hospitalId` always wins (a client-supplied value is silently ignored, never trusted — docs/18-MULTI-TENANCY.md point 3); a Super Admin (`hospitalId` is `null` on their own token) must name one explicitly (body field on writes, `?hospitalId=` query param on reads) or the request is rejected with `VALIDATION_ERROR` rather than silently defaulting to "every hospital." `DepartmentsService.create`/`update` additionally cross-check a supplied `branchId` actually belongs to the resolved hospital (docs/18 point 4, defense-in-depth against a cross-tenant object reference).
- **Staff invite → activate → deactivate**, resolving a genuine gap in docs/16-AUTHENTICATION.md's one-line "Staff activation" description into a concrete, tested protocol (and updating that doc section to match, since the original text under-specified it): `POST /users/invite` creates a `PENDING_ACTIVATION` `User` (+ linked `Staff` row for Nurse/Receptionist/Admin roles — a Doctor invite creates neither `Staff` nor `Doctor`, deferred to Phase 5's `POST /doctors`) and signs a 72h RS256 **activation token** (new `ActivationTokenService`, `apps/api/src/common/jwt/`, same keypair as access tokens, distinguished by a `typ: "activation"` claim so the two can never be confused). Completing it needs **two** more calls, mirroring `/auth/forgot-password` → `/auth/reset-password`'s existing shape: `POST /users/activate/request-otp` (new endpoint, added to docs/15-API-SPECIFICATION.md in this pass) sends a fresh OTP under a new `ACCOUNT_ACTIVATION` `OtpChallenge` purpose (new enum value — additive migration `20260902120000_add_account_activation_otp_purpose`, docs/13-DATABASE-DESIGN.md updated); `POST /users/activate` verifies that code (cross-checked against the token's own `sub` so one invitee's challenge can never activate a different account), sets the password, and flips the user to `ACTIVE`.
- **Hospital suspension blocks login**: `Hospital.status = SUSPENDED` now rejects `POST /auth/login` with a new `AUTH_HOSPITAL_SUSPENDED` (403) — checked only *after* password verification, so a suspended hospital's existence is never an enumeration signal (docs/16-AUTHENTICATION.md "Login flow" and docs/28-ERROR-HANDLING.md updated). This is docs/18-MULTI-TENANCY.md's "What 'suspended' means for a Hospital" made real; existing sessions are not force-revoked (blocks new logins only — reversible, non-destructive, matching what's actually documented rather than inventing extra invasiveness).
- New error code `AUTH_ACTIVATION_TOKEN_INVALID` (400) for a missing/malformed/expired/wrong-purpose activation token — added to docs/28-ERROR-HANDLING.md and `packages/shared/src/errors/error-codes.ts` first, per the standing "codes are added there before code uses them" rule.
- `AuditInterceptor` (built but unused in Phase 3) got its first real callers here: `@Audit(...)` on every Hospital/Branch/Department mutation; `UsersService` calls `AuditService.record()` directly for invite/activate/deactivate/role-assign (needs before/after context the generic interceptor can't infer).
- **A real, previously-latent bug found and fixed while building this**: combining `@CurrentUser()` with a method-level `@UsePipes(schema)` silently runs the *actor* object through that same Zod schema too — `@Req()`/`@Res()` are exempt (Nest's built-in param types), but a custom `createParamDecorator()` like `@CurrentUser()` is not. Every `list`/`create`/`invite` handler that combined the two (plus `revokeSession` in `AuthController`, dating back to Phase 3 and never caught because no test exercised it) was hard-failing every request with `VALIDATION_ERROR`. Fixed by switching to per-parameter pipes (`@Body(new ZodValidationPipe(...))`) everywhere this pattern occurs — caught immediately by this phase's own integration tests (every write returned 400 before the fix), which is exactly why those tests exist.
- Verified via 34/34 passing integration tests, new: `apps/api/test/tenant-management.integration-spec.ts` (Hospital/Branch/Department CRUD, cross-tenant 404s, Super-Admin-required-hospitalId, hospital-suspension-blocks-login) and `apps/api/test/users.integration-spec.ts` (full invite→request-otp→activate→login lifecycle, Doctor-invite-skips-Staff-row, duplicate-email 409, wrong-purpose-OTP rejection, deactivate-revokes-sessions-immediately, tenant isolation, non-manager 403) — plus the existing 16 auth + 17 unit tests, all still green. New fixture helpers in `apps/api/test/fixtures.ts` sign real access tokens via the same `AccessTokenService`/`AuthzResolverService` the login flow uses, rather than hand-rolling test-only JWTs.

**Phase 4 — Admin (`apps/admin`):** the authenticated shell (`src/app/(shell)/layout.tsx` + `src/components/shell/`) — permission-filtered sidebar (a nav item only renders if `user.permissions` includes it, and only for modules that actually exist yet: Branches/Departments/Users, not the full 17-item docs/09 mockup sidebar most of which isn't built until later phases), topbar with a Super-Admin-only Hospital switcher (`useHospitalScope()` context) and a working Log out — plus Branches, Departments, and Users screens (list + create/invite via a shared `Modal` primitive, Deactivate action) and a public Activate Account page (`/activate-account?token=...`, mirrors the request-otp → activate two-step flow).
- Session model gained `hospitalId` on `SessionUser` (both admin and the `/auth/login`/`/auth/me` responses now carry it — needed for the Hospital switcher and for an Admin's implicit tenant scope) and a real `logout()` (clears the sealed refresh cookie via a new `POST /api/session/logout` route; does **not** call the backend's session-specific revoke, since the client never holds a `DeviceSession` id to pass it — documented as a known simplification, not silently swept under the rug).
- New shared primitives in `packages/ui/web`: `Modal`, `SelectField`. Data fetching/mutation via `@tanstack/react-query` (first real use — a `QueryProvider` added to the root layout) calling a small typed resource layer (`src/lib/resources.ts`) that appends `?hospitalId=` for a Super Admin caller and lets an Admin's own hospital resolve server-side.
- Verified three ways, escalating: 28 passing component tests (mocked API layer, per-screen render/error/success coverage); a full `pnpm lint`/`typecheck`/`test` pass across all 11 packages; **and** a real, live end-to-end browser session — real `pnpm dev` API server (the Phase 3 fix), pglite-seeded demo data, actual login as the demo Hospital Admin, actual Branch creation, actual Department/User lists, actual staff invite — not mocks.
- **The live browser pass caught a second real bug** that 28 passing component tests, full typecheck, and full lint all missed: `DepartmentsService.list()` never included the `doctorDepartments` relation (only `getById()` did), but the admin Departments page unconditionally read `department.doctorDepartments.length` to show the per-row doctor count from docs/09's mockup — a guaranteed crash the instant real API data (missing the field) replaced the component tests' mocks (which always included it). Fixed on both sides: the list query now includes the relation (matching what the UI is actually supposed to show), and the render also gained `?.length ?? 0` as cheap insurance against the same class of bug recurring. This is the concrete case for why "verified via component tests" and "verified against a live API" are not interchangeable claims — recorded here rather than glossed over.

**Two schema/doc updates that came out of implementing the above** (per `CLAUDE.md`: update the doc, don't silently diverge): docs/16-AUTHENTICATION.md's "Staff activation" paragraph was a single under-specified sentence — rewritten to the precise two-call sequence actually implemented and tested; docs/15-API-SPECIFICATION.md's `/users` table gained the `POST /users/activate/request-otp` row this required.

**Phase 5 — Backend (`apps/api`):** `DoctorsModule`, `PatientsModule`, `StaffModule` — three new domain modules, wired into `AppModule`.
- **Schema gap found and fixed before any Phase 5 code was written**: docs/09-ADMIN-DESIGN-MOCKUPS.md's Patient Details "Overview" tab shows `Registered Branch: Main Branch · Since 12 Jan 2024`, but `Patient` had no branch/hospital association field at all — a Patient's own identity is deliberately never hospital-scoped (docs/18-MULTI-TENANCY.md "Patient identity is the one platform-level exception"). Added `Patient.registeredHospitalId`/`registeredBranchId` (both nullable, additive migration `20260903090000_add_patient_registration_scope`) — a narrower, separate concept from clinical `hospitalId` scoping: which hospital's front desk registered the patient, not where their clinical data lives. Documented in docs/13-DATABASE-DESIGN.md and docs/18-MULTI-TENANCY.md.
- **Scope enforcement precisely matches docs/02-PERSONAS-AND-ROLES.md's role → permission matrix**, not just "hospital-scoped for everyone" (Phase 4's simpler domains — Branch/Department — happen to be HOSPITAL-scoped for every role that can see them, so this distinction didn't come up until now):
  - `doctors.read`: `PATIENT`/`SUPER_ADMIN` are `PLATFORM` (cross-hospital discovery — a patient browsing the doctor directory to book is legitimate, not a leak; `hospitalId` only narrows, never required), `ADMIN` is `HOSPITAL` (own hospital, JWT-derived, never a client override), `RECEPTIONIST` is `BRANCH` (narrower than their own hospital — resolved by looking up the caller's own `Staff.branchId`), `NURSE`/`DOCTOR` hold no `doctors.read` permission at all (blocked at the guard, never reaches the service).
  - `patients.read`/`write`: `self` (PATIENT, via `/patients/me`), `branch` (NURSE read-only, RECEPTIONIST read+write — resolved the same way as doctors' branch scope), `assigned` (DOCTOR — **deliberately returns an empty list / `NOT_FOUND` rather than approximating with a broader scope**, since an actual appointment/consultation relationship is the only correct filter and that data doesn't exist until Phase 7), `hospital` (ADMIN), `platform` (SUPER_ADMIN).
  - `staff.read`/`write`: `HOSPITAL` for ADMIN, `PLATFORM` for SUPER_ADMIN, no other role — matches `StaffService`'s plain `resolveHospitalId` reuse (no BRANCH-scope complexity needed here, unlike doctors/patients).
- **A real scope-enforcement gap found and fixed during this phase's security review**: `PatientsService.register()`'s first draft let a Receptionist supply an explicit `branchId` in the request body, checked only against "belongs to the target hospital" — meaning a Receptionist could register a patient into a *different branch of their own hospital* than the one they actually work at, even though `patients.write` is `BRANCH`-scoped for them (docs/02's matrix), not hospital-scoped. Fixed to ignore any client-supplied `branchId` for a Receptionist actor entirely and always resolve their own `Staff.branchId` server-side — the same "never trust a client override for a scoped actor" rule `resolveHospitalId` already enforces for `hospitalId`, now applied consistently to `branchId` too.
- **A pre-existing, unrelated security defect found and fixed during this phase's security review, affecting Phase 3/4 code as much as Phase 5's**: every endpoint returning a `User` relation (`GET /users`, and now `GET /doctors`, `GET /patients`, `GET /staff`) was serializing the full `User` row — **including `passwordHash`** (argon2id-hashed, but still a credential that should never leave the server) — into the JSON response, confirmed empirically via a live network capture during this phase's browser verification. Root cause: no query anywhere used `select`/`omit` to exclude it, and Prisma has no schema-level "never select this column" concept. Fixed at the client-factory level, not per-query: `packages/database/src/client.ts`'s `createPrismaClient()` now passes `omit: { user: { passwordHash: true } }` to the Prisma constructor (needs `previewFeatures = ["omitApi"]` in `schema.prisma`'s `generator client` block, added in this pass) — every current and future read path that touches `User` is safe by default, with no per-query discipline required. The one legitimate exception, `AuthService.login()`'s password check, explicitly opts back in with `omit: { passwordHash: false }` on that one query. Verified two ways: all 52 integration tests (including full login flows) still pass, and a live `curl` against `/users`, `/doctors`, `/patients`, `/staff` after the fix confirms zero occurrences of `passwordHash` in any response body. Documented in docs/44-CODING-STANDARDS.md so no future query re-introduces this by instantiating a bare `PrismaClient`.
- **Doctor profile creation deliberately never creates a `User`** — `POST /doctors` attaches a clinical profile to a `User` already invited with the `DOCTOR` role via Phase 4's `POST /users/invite`, matching docs/15-API-SPECIFICATION.md's "Create (links to invited User)". Defense-in-depth checks (docs/18-MULTI-TENANCY.md point 4) confirm the target user actually holds the `DOCTOR` role, belongs to the resolved hospital, and doesn't already have a `Doctor` profile before attaching one.
- **Patient registration reuses Phase 4's exact activation infrastructure** — `POST /patients` (staff-initiated registration) creates a `PENDING_ACTIVATION` `User` (`hospitalId: null`, per the platform-level-identity rule above) + linked `Patient` row and signs the same `ActivationTokenService` token Phase 4 built for staff invites; completion goes through the same role-agnostic `POST /users/activate/request-otp` → `POST /users/activate` endpoints (they only ever checked `status === PENDING_ACTIVATION`, never role, so no new activation route was needed).
- **Staff deactivation reuses Phase 4's session-revocation pattern**: `StaffService.deactivate()` (and `PATCH /staff/:id` with `status: "INACTIVE"`, which delegates to the same method) sets the linked `User.status = DISABLED` and calls `RefreshTokenService.revokeAllForUser()`, mirroring `UsersService.deactivate()` exactly — a bare `Staff.status` flip without revoking sessions was the discovered-and-fixed gap for `User` deactivation back in Phase 4; this phase applies that lesson from the start rather than re-discovering it. A separate "Force Logout All Sessions" admin action implied by docs/09's Staff Details mockup was deliberately **not** built — `DELETE /auth/sessions?userId=` doesn't exist in docs/15-API-SPECIFICATION.md, and inventing it would violate `CLAUDE.md`'s standing rule; folding logout into deactivate covers the same real need.
- Verified via 18 new passing integration tests (`apps/api/test/doctors-patients-staff.integration-spec.ts`) — Doctor create/update/department-assign-remove, tenant isolation, Receptionist branch-scoped doctor discovery, Patient PLATFORM-scope discovery for a Patient actor, Doctor's empty-list/404 ASSIGNED-scope behavior, Patient self-service via `/patients/me` vs. blocked cross-patient access via `/patients/:id`, Staff update/deactivate/tenant-isolation/permission-rejection — plus 22 new `packages/validation` schema unit tests and the full existing 34 integration + 17 unit tests, all still green (52 integration total). New fixture helpers in `apps/api/test/fixtures.ts`: `createBranch`, `createStaffMember`, `createDoctorProfile`, `createPatientProfile`.

**Phase 5 — Admin (`apps/admin`):** Doctors, Patients, Staff screens (`src/app/(shell)/{doctors,patients,staff}`), following Phase 4's established list + `Modal`-based create/edit pattern exactly rather than the full separate "Details" screen implied by docs/09-ADMIN-DESIGN-MOCKUPS.md (the same simplification Phase 4 already made for Users — no separate "User Details" route exists either). `T-504`/`T-505`/`T-506` are marked done against this simplified scope, noted explicitly in docs/41-TASKS.md rather than silently reinterpreting the task.
- **Doctors**: list (name, qualifications, department badges with inline remove, status dropdown, inline "assign department" select per row) + an "Add Doctor" modal that only offers Doctor-role `User`s not yet attached to a `Doctor` profile (client-side filtered: fetches Doctor-role users via `usersApi.list(..., { role: "DOCTOR" })` and excludes any whose `id` already appears in the Doctors list).
- **Patients**: list (name, contact, registered branch, status) + a "Register Patient" modal calling the new `POST /patients`.
- **Staff**: list (name, job title, branch, status) + inline Edit modal (job title/branch) + Deactivate action, mirroring the Users page's Deactivate pattern.
- New resource-layer types/calls in `src/lib/resources.ts`: `Doctor`/`StaffMember`/`Patient` interfaces, `doctorsApi`/`staffApi`/`patientsApi`, and `usersApi.list()` gained an optional `{ role, status }` filter (needed for the Doctors page's invited-user lookup). Three new sidebar entries in `src/components/shell/nav-items.ts`.
- **A real bug found and fixed during this phase's own component-test pass** (not a live-browser-only catch this time): `react-hook-form`'s `register(field, { valueAsNumber: true })` turns an *empty* optional number input into `NaN`, not `undefined` — so leaving "Years of experience"/"Consultation fee" blank on the Add Doctor form, or "Phone"/"Date of birth" blank on Register Patient, failed the (correctly optional) Zod schema with a confusing "Expected number, received nan" / "Invalid date" error instead of just omitting the field. Fixed with `setValueAs: (v) => (v === "" ? undefined : ...)` on every optional field of this shape — a form-UX gap that plain typecheck/lint could never catch, since the schemas themselves were already correct.
- Verified three ways again: 10 new passing component tests (38 total, up from 28) covering empty-state/list-render/create-or-register flows per screen; full `typecheck`/`lint` across the affected packages; **and** a real, live end-to-end browser session against a running `next dev` + the pglite-seeded demo data — actual login as the demo Hospital Admin, actual "Invite User" (role: Doctor) → "Add Doctor" → assign-department flow completed end-to-end, actual "Register Patient" flow completed end-to-end (new patient immediately visible in the list with `PENDING_ACTIVATION` status), actual Staff Edit modal opening with the right fields. The demo seed script (`packages/database/seed/demo/index.ts`) was also updated to populate the new `registeredHospitalId`/`registeredBranchId` fields on its 40 generated patients (distributed round-robin across both demo hospitals' branches, with roughly 1-in-6 left unregistered to demonstrate the self-registered-via-mobile case) — without this, the live Patients screen would have shown "No patients registered" despite 40 patients existing, since none of them had been registered anywhere under the old (pre-Phase-5) seed logic.

**Phase 6 — Backend (`apps/api`):** `SchedulesModule` — one controller (`SchedulesController`) over three services: `DoctorScheduleService` (weekly template), `ScheduleExceptionService` (one-off overrides), `AvailabilityService` (computed open slots). Route order matters here too: `/schedules/availability` is declared ahead of `/schedules/:doctorId` for the same reason `/users/activate*` was in Phase 4.
- **Schema gap found and fixed before any Phase 6 code was written, same pattern as Phase 5's `Patient.registeredHospitalId`**: availability computation needs to convert a doctor's wall-clock `DoctorSchedule.startTime`/`endTime` into real UTC instants (docs/19-APPOINTMENT-ENGINE.md, docs/04-FEATURE-SPECIFICATION.md's explicit "timezone correctness" test requirement) — but no field anywhere recorded *which* timezone a hospital's wall-clock times are even in. Added `HospitalSettings.timezone` (IANA zone string, default `"UTC"`, additive migration `20260909100000_add_hospital_settings_timezone`) rather than guessing one from a Branch's address fields. Documented in docs/13-DATABASE-DESIGN.md.
- **A second, unrelated pre-existing gap surfaced by the same need**: `HospitalsService.create()` (Phase 4) never actually provisioned a `HospitalSettings` row for a hospital created through the real `POST /hospitals` API — nothing before Phase 6 ever read `HospitalSettings`, so this went unnoticed for two phases. Fixed: `create()` now provisions one (all defaults, including the new `timezone`) in the same transaction, attributed to the creating Super Admin (`updatedBy`) — which needed `HospitalsController.create()` to gain `@CurrentUser()`, and, per the exact Phase 4 latent-bug pattern (`@CurrentUser()` + a method-level `@UsePipes()` silently re-validates the actor object against the body schema), switched that one route to a per-parameter pipe while leaving the controller's other three routes alone.
- **Availability computation is a genuinely pure function** (`apps/api/src/schedules/availability.util.ts`'s `computeAvailability()`), per docs/19's explicit framing — no Prisma/NestJS types appear in that file at all. `AvailabilityService` does all the DB reads (schedule rows, applicable exceptions via `doctorId` OR `branchId`-when-null, and a padded pre-filter over `Appointment` — always empty today, Phase 7 populates it) and hands plain objects to the pure function, which is why `T-605`'s buffer/exception/timezone/DST coverage could be written as fast, DB-free unit tests (see Testing status).
- **Resolved four genuinely ambiguous spec points, each documented inline in code and here rather than silently picked**:
  1. `ScheduleException.type` × `startTime`/`endTime` semantics: `HOLIDAY` is always full-day (rejected if given times); `LEAVE` is full-day when times are absent, a *blackout carved out of* the normal window when present; `REDUCED_HOURS`/`EXTENDED_HOURS` *replace* the day's normal window with the given one (required, can't be absent for these two types).
  2. `PUT /schedules/:doctorId` "replace weekly template" (docs/09's mockup shows one current template, not a history) means exactly that: every save deletes the doctor's existing `DoctorSchedule` rows and inserts the new set with `effectiveFrom = today`, `effectiveTo = null` — no per-row effective-date-range editing exposed in Phase 6's UI.
  3. `POST /schedules/:doctorId/exceptions`'s body never accepts `doctorId`/`branchId` — the path's `:doctorId` is the only target, since docs/15-API-SPECIFICATION.md documents no route for creating the model's other case (a branch/hospital-wide holiday, `doctorId = null`). `AvailabilityService` already honors that case if a row exists (e.g. seeded directly) — there's just no Phase 6 endpoint that creates one, per `CLAUDE.md`'s rule against inventing undocumented routes.
  4. The mockup's exception-conflict "Notify & Reschedule"/"Notify & Cancel" resolution flow needs `Appointment` reschedule/cancel endpoints that don't exist until Phase 7. New error code `SCHEDULE_EXCEPTION_CONFLICT` (409, added to docs/28-ERROR-HANDLING.md first): creating an exception that overlaps existing non-terminal appointments is rejected outright for now. The check is real (queries `Appointment`, timezone-aware window), not a stub — it simply can never fire yet, since nothing creates appointments before Phase 7.
- **Scope enforcement precisely follows docs/02's matrix, splitting one permission string into two different behaviors** — `schedules.read`'s `PATIENT` grant is annotated "PLATFORM, availability only" and this phase takes that literally: `GET /schedules/availability` is open to any role holding `schedules.read` at all (no `hospitalId` needed, mirrors `doctors.read`'s platform scope exactly — the response carries no PII); `GET/PUT /schedules/:doctorId` and the `/exceptions` sub-routes are a narrower surface (`DOCTOR` `SELF`, `NURSE`/`RECEPTIONIST` `BRANCH`, `ADMIN` `HOSPITAL`, `SUPER_ADMIN` `PLATFORM`) that a `PATIENT` actor is blocked from entirely (404) despite nominally holding the permission — enforced by one shared function, `apps/api/src/schedules/schedule-access.util.ts`'s `loadDoctorInScope()`. Documented in docs/18-MULTI-TENANCY.md alongside Phase 5's `doctors.read` note.
- **A real discovery gap found while designing the Admin UI, fixed before it became a blocker**: `DOCTOR` holds `schedules.write` (`SELF`) but no `doctors.read` at all (Phase 5's matrix) — so a doctor-role admin-console session had no way to learn its own `Doctor.id` to target `PUT /schedules/:doctorId` in the first place. Fixed by adding `doctorId` (own `Doctor.id`, `null` if none) to `GET /auth/me`'s response — an additive field on an existing, already-`@Authenticated()`-only, self-info endpoint, not a new route or permission; documented in docs/15-API-SPECIFICATION.md's `/auth` table. `AuthService.issueSession()` (shared by login/register) gained the same field for consistency.
- **A real bug caught by this phase's own live verification (curl, not the browser — see "Known issues")**: the raw `GET /schedules/:doctorId` and `.../exceptions` responses were serializing Prisma's `@db.Time`/`@db.Date` columns as full ISO datetime strings with a meaningless `1970-01-01` date component (e.g. `"1970-01-01T09:00:00.000Z"` instead of `"09:00"`) — harmless for `GET /schedules/availability`'s response (those genuinely are full instants) but wrong for the template/exception rows, which are supposed to carry plain wall-clock/date values. Fixed with `serializeDoctorSchedule()`/`serializeScheduleException()` (`schedule-access.util.ts`), applied in both services before any response leaves the API.
- Verified via 15 new passing integration tests (`apps/api/test/schedules.integration-spec.ts`) — template SELF/HOSPITAL/BRANCH/tenant-isolation scope, overlap rejection, wrong-department rejection, exception create/list/delete + tenant isolation + cross-doctor-delete rejection, availability computation against a real seeded template (timezone-correct) and reachable by a Patient actor with zero hospital context — plus **15 new DB-free unit tests** (`availability.util.spec.ts`) covering exactly `T-605`'s scope: buffer stride, `maxAppointments` cap, all four exception-type behaviors, branch-wide-exception branch matching, booked-slot exclusion, `effectiveFrom`/`effectiveTo` boundaries, correct UTC offset for a non-UTC zone, and a real DST-transition date (`America/Chicago`, 2026-03-08) producing `-06:00` before and `-05:00` after. Plus 17 new `packages/validation` schema unit tests, and the full existing 67 integration + 32 unit tests, all still green.

**Phase 6 — Admin (`apps/admin`):** Doctor Schedules screen (`src/app/(shell)/doctor-schedules`) — one combined page matching docs/09-ADMIN-DESIGN-MOCKUPS.md's "Doctor Schedules" mockup exactly (Weekly Template + Upcoming Exceptions on one screen; the mockup's separate "Schedule Exceptions" block is the Add Exception form, not a second list screen). `T-604` is marked done against this scope — the mockup's Notify-and-resolve conflict UI is explicitly deferred to Phase 7 alongside its backend counterpart, noted in docs/41-TASKS.md rather than silently reinterpreting the task.
- Doctor selector for Admin/Nurse/Receptionist/Super Admin (reuses Phase 5's `doctorsApi.list()`); for a `DOCTOR`-role session, the selector is skipped entirely and the page self-targets `session.user.doctorId` (the new `/auth/me` field) — `doctorsApi.list()` is never called for that role, matching that it holds no `doctors.read` at all.
- Edit Template modal uses `react-hook-form`'s `useFieldArray` (new to this codebase) to add/remove day-blocks freely; department choices come from the full `Doctor` record when available (Admin/staff path) or are derived from the doctor's own existing template rows when not (the only source a `DOCTOR`-self session has) — documented in code as an accepted MVP limitation: a brand-new self-service doctor with zero existing schedule rows and no Admin-set department yet cannot self-edit until one exists, which in practice never happens since Phase 5's doctor-creation flow already assigns departments up front.
- Read-only visitors (`NURSE`/`RECEPTIONIST`, `schedules.read` without `schedules.write`) see the template/exceptions but not the Edit Template/Add Exception/Remove controls — the **first screen in this codebase where a role can reach a page without holding every action on it**, so `hasPermission()` (existing helper, previously only used for sidebar nav gating) is now also used inline to hide write-only controls; every earlier phase's screens happened to have exactly one role per screen who could both see and act, so this need never came up before.
- New resource-layer types/calls in `src/lib/resources.ts`: `DoctorScheduleRow`/`ScheduleExceptionRow`/`AvailabilityResult`, `schedulesApi`. New sidebar entry gated by `schedules.read`.
- Verified three ways: 7 new passing component tests (empty states, template/exception listing, Edit Template save, Add Exception create, exception removal, `DOCTOR`-self auto-select skipping `doctorsApi.list()`, read-only-actor button hiding); full `typecheck`/`lint`; **and** direct `curl` verification against the real running API + pglite-seeded demo data (the browser pane itself became unresponsive mid-session under severe host memory pressure — see "Known issues" — so this phase substitutes a full request/response round-trip against real seeded data for the usual screenshot pass). The demo seed script already populated realistic `DoctorSchedule`/`ScheduleException` rows for every doctor since Phase 1/2 (a forward-looking scaffold choice that happened to need nothing further from this phase) — confirmed live: a real Cardiology doctor's Monday template (09:00–13:00, 14:00–18:00, 20-min slots, 5-min buffer) round-tripped through `GET /schedules/:doctorId` with clean `"09:00"`/`"13:00"` values (post-fix) and through `GET /schedules/availability` with correctly `-05:00`-offset (CDT, August) slots that correctly skip the 13:00–14:00 gap and space 5 minutes apart.

**Phase 7 — Backend (`apps/api`):** `AppointmentsModule` — `AppointmentsService` (list/detail/booking) and `AppointmentTransitionsService` (reschedule/cancel/checkin/no-show), sharing one scope-access utility (`appointment-access.util.ts`'s `loadAppointmentInScope()`, the prerequisite gate for every mutation) and one policy utility (`appointment-policy.util.ts`, pure functions over `HospitalSettings`' booking-policy fields — no Prisma/NestJS types, same "pure function" convention as Phase 6's `availability.util.ts`). Imports `SchedulesModule` (now exports `AvailabilityService`) to re-run the exact same availability computation `GET /schedules/availability` uses as the server-side re-validation of a requested slot — the single source of truth for "is this slot real," per docs/19-APPOINTMENT-ENGINE.md.
- **Two real pre-existing schema gaps found and fixed before any Phase 7 code was written**: `Appointment.branchId` had no foreign-key constraint at all (a plain column, never wired to `Branch` in the init migration) — added the relation to `schema.prisma` and a new additive migration, `20260910090000_add_appointment_branch_fkey`. Initially suspected the partial unique index itself (`appointments_doctor_id_start_time_active_key`, the actual no-double-booking mechanism) was also missing — turned out it was already present in the init migration, just under review; confirmed applied and working via this phase's own concurrency test.
- **Booking transaction** (`AppointmentsService.create()`) exactly follows docs/19's numbered steps: resolve `hospitalId`/`patientId`/`branchId` from the actor (never trust a client-supplied `patientId` for a self-service `PATIENT` booking — same "own scope always wins" rule every prior phase's write path uses), validate the doctor is active and assigned to the requested department, re-run `AvailabilityService.computeForDoctor()` for the requested date and match the exact requested `startTime` against a real generated slot (`APPOINTMENT_NOT_AVAILABLE` if none matches — this is the actual re-validation, not a stub), check `HospitalSettings.minBookingLeadMinutes`/`maxAdvanceBookingDays`, then open a transaction: a row-level `SELECT ... FOR UPDATE` over the doctor's existing non-terminal appointments that day (only when `DoctorSchedule.maxAppointments` is set) re-checks the daily cap before inserting, then inserts the `Appointment` (status `CONFIRMED` or `SCHEDULED` per `HospitalSettings.autoConfirmBookings`) and its `CREATED` `AppointmentHistory` row together. A `Prisma.PrismaClientKnownRequestError` with code `P2002` (the partial unique index's unique-violation, translated by Prisma regardless of the index not being declared in `schema.prisma` — confirmed empirically, not assumed) is caught and mapped to `409 APPOINTMENT_CONFLICT`; the notification stub fires only after commit, per docs/19 step 8.
- **Reschedule** (`AppointmentTransitionsService.reschedule()`) is a single atomic `UPDATE` of the existing row's `startTime`/`endTime`/`rescheduleCount` (never delete+recreate, preserving id/history) — a unique-violation here maps to `422 APPOINTMENT_NOT_AVAILABLE`, **not** `409 APPOINTMENT_CONFLICT`, per docs/19's explicit distinction ("the original appointment is untouched"). Blocked once `CHECKED_IN`/`IN_PROGRESS` (rescheduling something already in progress doesn't make sense) and once `maxReschedulesPerAppointment` is reached (hard cap, no staff override — the window itself, `rescheduleWindowMinutes`, *is* staff-overridable with a mandatory reason).
- **Cancel** sets a terminal `CANCELLED` status (immediately freeing the slot — no separate release step needed, since the partial index only covers non-terminal statuses) and records `isLateCancellation` when done inside `cancellationWindowMinutes`. A self-service `PATIENT` is blocked entirely from cancelling once `CHECKED_IN`/`IN_PROGRESS` (they're already there); staff can still cancel in that state if genuinely needed.
- **Check-in** assigns `queueNumber` via a row-level `SELECT ... FOR UPDATE` over existing checked-in appointments for the same `(branchId, departmentId, day)`, exactly the pattern docs/19 names — with the same documented caveat as docs/19's own description: locking *existing* rows can't serialize the very first two check-ins of a day against each other if none exist yet to lock (a narrower, explicitly-accepted gap than the booking transaction's own guarantee, which is airtight via the database constraint regardless).
- **The `appointments.update` permission conflates two different sub-actions** behind one guard-level check, resolved via docs/02-PERSONAS-AND-ROLES.md's per-role prose (not the quick-reference matrix, which only lists a subset of permissions): reschedule (`PATIENT` self only, `RECEPTIONIST`/`ADMIN`/`SUPER_ADMIN`) and no-show (`DOCTOR`, `RECEPTIONIST`/`ADMIN`/`SUPER_ADMIN` — never `PATIENT`). `NURSE` holds neither (no `appointments.update` at all). Enforced with an explicit role-allowlist inside each transition method, on top of `loadAppointmentInScope()`'s scope check — the same layered pattern as every prior phase's guard-permission-plus-service-scope discipline, just with a third layer (sub-action) this phase introduces for the first time. No-show additionally requires the appointment be `SCHEDULED`/`CONFIRMED` (not already checked in) and that `NO_SHOW_GRACE_MINUTES` (15, not hospital-configurable per docs/19) has elapsed since `startTime`.
- **A real doc contradiction found and resolved before writing the seed data**: docs/02's `PATIENT` prose permission list omitted `appointments.checkin`, but the quick-reference matrix table explicitly grants it at `SELF` scope (matching docs/08's mobile "Check-in (modal)" screen and this phase's own `T-710` task scope) — the seed catalog (`packages/database/seed/catalog/roles.ts`) had inherited the same omission, so a patient's JWT never actually carried the permission despite the UI needing it. Resolved by trusting the matrix (the more specific, more recently-touched source) and fixing both the doc prose and the seed grant in the same change, per `CLAUDE.md`'s "resolve the contradiction, don't silently diverge" rule.
- Notification stub (`NotificationStubService.trigger()`, `T-711`) — a `console.log`-based no-op, same established pattern as `OtpService.deliver()`'s dev stub; real delivery is Phase 10.
- Verified via 28 new passing integration tests (`apps/api/test/appointments.integration-spec.ts`) plus 17 new DB-free unit tests (`appointment-policy.util.spec.ts`). **The concurrency test (`T-706`, E2E-APPT-02) is real, not simulated**: two patients' `POST /appointments` for the exact same `(doctorId, startTime)` fired via `Promise.all()` against a live database resolve to exactly one `201` and one `409 APPOINTMENT_CONFLICT`, and a direct row count confirms exactly one `Appointment` exists afterward — this is the concrete proof that the partial unique index (not the application-level availability check) is what makes double-booking actually impossible, per docs/19's own framing.
- **A real access-control bug found by this phase's security review, fixed before merge**: `AppointmentsService.create()`'s branch-scope check for a `RECEPTIONIST` booking on a patient's behalf used `if (staffBranchId && staffBranchId !== branchId)` — since `Staff.branchId` is nullable, a Receptionist with no branch assignment (a real, reachable state, not hypothetical) had the check silently skipped and could book against *any* branch in their hospital, contradicting `appointments.create`'s `BRANCH` scope and the fail-closed pattern every other scope check in this same module uses (`loadAppointmentInScope()`, `resolveListWhere()`). Fixed to fail closed (`if (!staffBranchId || staffBranchId !== branchId)`), matching the rest of the module; a dedicated regression test (an unassigned Receptionist attempting a cross-branch booking, expecting `400 VALIDATION_ERROR`) was added alongside the fix.

**Phase 7 — Admin (`apps/admin`):** Appointments and Calendar screens (`src/app/(shell)/{appointments,calendar}`), plus a shared `AppointmentDetailsModal` (`src/components/appointments/appointment-details-modal.tsx`) reused by both — the first cross-screen shared component in this codebase's admin console (every prior phase's modals were screen-local).
- **Appointments**: list with Doctor/Status/date-range filters (docs/09's free-text "🔍 Search" chip is not backed by any documented `/appointments` query filter — docs/15's route table lists `status`/`doctorId`/`patientId`/`branchId`/`departmentId`/`from`/`to`/`view` only, so it's omitted rather than invented) + a New Appointment modal (doctor → department → patient → date → real-availability-backed time-slot select, calling the same `GET /schedules/availability` the booking flow's correctness depends on, not a fabricated slot list) + row click → `AppointmentDetailsModal` (Reschedule/Cancel/Check In/Mark No-Show, gated by the same permission + sub-action-role logic the backend enforces — client-side gating is UX only, the server remains authoritative).
- **Calendar**: a simplified day-view grid (doctor columns × time-of-day rows derived from that day's actual appointments, status-color-coded via `packages/ui/tokens`' existing `appointmentStatusColor` map — pre-authored in Stage 1 for exactly this use), not the mockup's full Day/Week/Month view with drag-to-reschedule — a deliberate scope trim matching the same "cover the load-bearing daily-use case, defer the rest" pattern Phase 6 applied to its own Calendar-adjacent mockups.
- **Appointment Details is a modal, not a separate route** — continuing every prior phase's list+modal convention rather than docs/09's implied back-navigation "Details screen" pattern.
- **A real gap in the design-token wiring found and fixed while building Calendar's status colors**: `apps/admin/tailwind.config.ts` only ever mapped a subset of `packages/ui/tokens`' color palette into Tailwind class names (`primary`/`on-primary`/`primary-container`/`secondary`/`tertiary`/`error`/`success`/`warning`/`surface`/`surface-container`/`on-surface`/`on-surface-variant`/`outline` — missing every other `on-*`/`*-container` pair, and `surface-variant` itself). This was silently already in use by Phase 5's Doctors page (`bg-surface-variant` on department badges — a real, pre-existing, unnoticed styling gap, not something this phase introduced), so extending the config to the full token set fixes that too. Documented as a "propagates automatically" config, now actually complete.
- Verified three ways: 9 new passing component tests (54 total, up from 45) — empty states, filtered list rendering, New Appointment booking flow (including real availability-driven slot selection), Details-modal open + Cancel-confirm flow (Appointments page), doctor-column rendering + click-through-to-Details + day-navigation re-fetch (Calendar page); full `typecheck`/`lint`; and the same live `curl`-based verification pattern as Phase 6 for the underlying API (see Backend section's concurrency-test note — the admin screens consume the identical, already-proven-correct endpoints).

**Phase 7 — Mobile (`apps/mobile`):** the first authenticated screens past Splash — Home (minimal entry point), Search Doctors/Departments, Doctor Profile, Department Details, a combined Doctor-Availability/Select-Date/Select-Time screen (reused for both new bookings and Reschedule, per docs/08's own "identical component" framing), Booking Confirmation (with an inline success state), Appointments (Upcoming/History tabs), Appointment Details (with inline Cancel and Check-in modals) — under a new `app/(app)/` auth-gated route group (`app/(app)/_layout.tsx`, mirroring Splash's own bootstrap-aware redirect logic) and a `QueryProvider` newly wired into the root layout (`@tanstack/react-query`'s first real use in mobile, mirroring admin's Phase 4 pattern).
- **Home is deliberately *not* docs/08's full "Patient Home" dashboard** (upcoming-appointment card, recent reports, notification bell, four-tab bottom nav) — that needs data this phase doesn't own (reports, notifications) and is tracked separately as `T-1301` (Phase 13). This phase's Home is a minimal greeting + three entry points (Find a Doctor / Browse Departments / My Appointments) + Log Out (temporarily homed here, since Profile/Settings don't exist yet either) — enough to make the booking flow reachable without building a dashboard this phase has no real data to populate.
- **"Upcoming"/"History" is a client-side split**, not a `status=upcoming` API filter — docs/08's mockup implies that query value, but `Appointment.status` (docs/13's actual enum) has no such member; the screen instead fetches the patient's own full list and partitions by non-terminal vs. terminal status client-side.
- **Doctor/hospital/branch names have a real, documented data-availability gap**: `GET /doctors` includes department names (via `doctorDepartments`) but never a hospital or branch name, and `PATIENT` holds neither `branches.read` nor `hospitals.read` to fetch them separately (docs/02's matrix) — so Doctor Profile and the search screens show department names only, not docs/08's "City General Hospital · Main Branch" line. Once an `Appointment` exists, its own response *does* bundle `branch`/`department` objects (no separate permission needed, since they're relations on a resource the patient already has `appointments.read` for), so Appointment Details/Upcoming/History correctly show branch names. Not a bug to fix in Phase 7 — inventing new `PATIENT` permission grants outside docs/02's matrix would violate `CLAUDE.md`'s standing rule.
- **Star ratings and "Next available" times shown in docs/08's Search Doctors mockup are omitted, not fabricated** — `Doctor` has no rating/review field (docs/13), and a per-doctor "next available slot" in a list view would require an N+1 availability call per row with no backing list-level API; the real per-doctor availability lives on the (already-built) Doctor Availability screen instead.
- New shared primitives: `src/lib/resources.ts` (`doctorsApi`/`departmentsApi`/`schedulesApi`/`appointmentsApi`, mirroring admin's resource-layer shape), `src/lib/query-provider.tsx`, `src/lib/test-utils.tsx` (`renderWithQueryClient`, mirroring admin's identical helper), `src/components/doctor-card.tsx` (shared between Search Doctors and Department Details, to avoid one route file importing another's internals).
- Verified via 24 new passing component tests (42 total, up from 18) across 6 new spec files — Search Doctors (list/empty/navigate/department-filter), Doctor Profile (render/navigate-to-booking/no-department-fallback/not-found), Booking Confirmation (review/book-success/conflict-error/view-appointment-nav), Appointments list (upcoming/empty-CTA/history-tab/navigate), Appointment Details (render/check-in-with-queue-number/cancel-with-reason/hide-actions-once-checked-in/not-found), Home (greet/navigate/log-out) — plus full `typecheck`/`lint` and the existing 18 auth-stack tests, all still green. **No simulator/device available in this sandbox** (same carryover as Phase 3) — real-device concerns (actual navigation transitions between the new `expo-router` routes, real keyboard behavior on the new form screens) remain unverified beyond component-test coverage.

## Current feature

None in progress — Phase 7 is closed out. Next unit of work is Phase 8 (`T-801`: Consultation module — start/update/complete state machine).

## Known issues

- **Docker unavailable in this development environment** (carried over from Phase 2). All Phase 3–6 integration-test verification used `@electric-sql/pglite` + `pglite-socket` as a temporary WASM Postgres stand-in (never committed, recreated fresh per verification session), requiring two non-default Prisma connection-string flags (`?pgbouncer=true` to disable named prepared statements, `&connection_limit=1` to avoid the stand-in's concurrent-connection limitation) — both are artifacts of that specific tool, **not** requirements of the real docker-compose/Supabase Postgres this project targets. Confirm the full `pnpm quality` (including `test:integration`) against real Docker before trusting this as final proof.
  - **Sharper than previously documented, and worse this phase:** with `connection_limit=1`, the stand-in tolerates exactly **one** live Prisma client at a time, and `--runInBand` is required for `test:integration` against it (Jest's default parallel workers starve each other otherwise) — unchanged from Phase 5's note. **New this phase:** the *host machine itself* ran critically low on free memory for extended stretches (observed as low as ~0.6–1 GB free out of ~7.8 GB total, from causes outside this session's own processes) — this is a step beyond the already-documented "pglite is memory-sensitive" issue: at that point *any* new process (pglite, the API dev server, even the browser pane used for UI verification) could fail to start or hang, regardless of its own `--max-old-space-size` cap, because the OS itself had nothing left to give any process. The only reliable response was to retry after freeing something (stopping the admin dev server temporarily) or simply retry the exact same command a few seconds later — this is host-level resource contention, not a bug in any of pglite, the API, or the admin app, and not reproducible against a real deployed environment with dedicated memory.
  - **Also new this phase:** under this same pressure, the Claude Browser pane itself became unresponsive (`navigate`/`get_page_text` timing out at their ceiling) for the remainder of the session after the admin dev server was briefly stopped and restarted — Phase 6's admin-screen verification substitutes a direct `curl` round-trip against the real running API + real seeded data instead (see the Admin section above for exactly what that proved). A future session hitting an unresponsive Browser pane on this machine should suspect host memory pressure first, and `curl`/`read_network_requests`-style verification is a legitimate fallback, not a downgrade in rigor, when the plumbing has already been proven correct by other means (component tests + integration tests + a real request/response round-trip all agreeing, as here).
  - **New this phase, and worth knowing precisely:** the pglite stand-in reliably runs `test:integration` for **one spec file at a time** (`--runInBand`, plus explicitly setting `DATABASE_URL`/`DIRECT_DATABASE_URL` per invocation since nothing auto-loads `apps/api/.env` for a plain `jest` run) — all 6 spec files were verified this way, individually, all green. Running the *default* multi-file glob (`test:integration` with no path filter) against the same live stand-in reliably broke it partway through with `Can't reach database server at 127.0.0.1:55432` on a later file, even though the pglite process itself stayed alive and listening — restarting the pglite process (and re-running `prisma migrate deploy` against the fresh instance) immediately fixed it. Root cause not fully isolated (plausibly `connection_limit=1` plus the stand-in's `maxConnections` socket-server option interacting badly across 6 back-to-back full Nest-app boot/teardown cycles) — treat "run each integration spec file separately" as the reliable procedure on this host until Docker is available, not a one-off fluke.
- **A real, pre-existing security defect was found and fixed in Phase 5**: `passwordHash` was leaking into every API response that included a `User` relation, across Phase 3/4 code as much as Phase 5's new code — see docs/44-CODING-STANDARDS.md for the fix (`createPrismaClient()`'s client-wide `omit`). Recorded here again since it's exactly the kind of cross-cutting gap a future session should know was closed, not re-discover.
- **No simulator, Expo Go, or device available in this sandbox for `apps/mobile`.** The 6 new screens are verified via component tests only (React Native Testing Library) — real-device concerns (SecureStore's actual OS-level Keychain/Keystore behavior, real keyboard/`inputMode` behavior, actual navigation transitions, real network timing) are unverified. `expo start --web` was not attempted this pass; it's a plausible partial substitute (react-native-web) worth trying in a future session if a simulator remains unavailable.
- `packages/ui/web` and `packages/ui/native` were empty Phase 1 placeholders (`export {}`) until this phase — both now have their first real components (5 in each), sized exactly to what the auth screens needed. Expect them to grow again alongside Phase 4's admin shell (`T-405`) and Phase 5's patient-facing screens.
- Same carryover from Phase 1/2: the `jest-expo`/`expo-router` React 19 RC peer-dependency warning (harmless, `pnpm install` still succeeds).
- `/health/ready` now performs a real `$queryRaw` DB-reachability check (closed out from Phase 2's "known issue" list).
- OTP delivery is still a `console.warn`-based dev stub (`OtpService.deliver()`, clearly marked `[DEV ONLY]` in the code and docstring) — real email/SMS delivery is Phase 10's notification system. Do not treat this as production-ready; it's called out again here so it isn't missed.
- Mobile Login's "resume OTP verification for a pending-activation account" flow (docs/08) is intentionally not implemented — see the Mobile section above.
- **`PatientsService.resolveScope()`'s `DOCTOR` branch still returns `"NONE"`** (empty list / 404) rather than using the now-real `Appointment`/`Consultation` relationship data Phase 7 introduced — carried forward as a deliberate scope decision (see docs/18-MULTI-TENANCY.md's appointments-scope paragraph) rather than expanded mid-phase. A clean, small follow-up for a future pass: change it to query `Appointment`/`Consultation` for an actual doctor-patient relationship instead of hardcoding `"NONE"`.
- Mobile's Doctor Profile/Search screens cannot show a hospital or branch name (only department names) — see the Mobile section above for why; not a bug, a real `PATIENT`-permission-scope gap that would need a docs/02 change to close.

## Database migration status

Six migrations now: Phase 2's `20260825114948_init` (full schema, including the `appointments_doctor_id_start_time_active_key` partial unique index that makes booking conflict-safe), Phase 4's `20260902120000_add_account_activation_otp_purpose` (additive `OtpPurpose` enum value), Phase 5's `20260903090000_add_patient_registration_scope` (additive, nullable `Patient.registeredHospitalId`/`registeredBranchId` + FKs + indexes), Phase 6's `20260909100000_add_hospital_settings_timezone` (additive `HospitalSettings.timezone`, default `"UTC"`), and Phase 7's `20260910090000_add_appointment_branch_fkey` (additive — adds the `appointments.branch_id → branches.id` foreign key the init migration never had). All six were applied cleanly this phase to a fresh pglite stand-in via `prisma migrate deploy` (confirmed, not assumed) — still none applied to any real environment (no local Docker Postgres run, no Supabase project provisioned).

## API status

Live surface: `/auth/*` (full, from Phase 3; `GET /auth/me` gained `doctorId` in Phase 6), `/hospitals` (Super Admin only; `POST /hospitals` also provisions `HospitalSettings`), `/branches`, `/departments`, `/users` (incl. `/users/activate` + `/users/activate/request-otp`, both public), `/doctors`, `/patients` (incl. `/patients/me`), `/staff`, `/schedules` (`GET /schedules/availability`; `GET/PUT /schedules/:doctorId`; `GET/POST /schedules/:doctorId/exceptions`, `DELETE /schedules/:doctorId/exceptions/:id`), and new this phase: `/appointments` (`GET /appointments`, `GET /appointments/:id`, `POST /appointments`, `PATCH /appointments/:id/reschedule`, `PATCH /appointments/:id/cancel`, `POST /appointments/:id/checkin`, `PATCH /appointments/:id/no-show`) — global guard chain, exception filter, audit interceptor (real callers on every mutation), Swagger doc generation at `/api/v1/docs`. `pnpm --filter api dev` runs cleanly in this sandbox (Phase 3's `ts-node` fix). `build`/`start` (the `nest build`/`node dist/main.js` production path) remain unfixed — same root cause as before, out of scope again this phase, real gap before Render deployment (Phase 15). No other domain modules yet — Consultations/medical records are Phase 8.

## Mobile status

Auth stack (Splash → Welcome → Login/Register → OTP Verification → Forgot/Reset Password) plus, new this phase, a full authenticated `app/(app)/` route group: Home (minimal entry point, not the full dashboard — see Phase 7 Mobile section above), Search Doctors, Search Departments, Doctor Profile, Department Details, Doctor Availability/Select-Date-Time (shared with Reschedule), Booking Confirmation, Appointments (Upcoming/History), Appointment Details (with Check-in/Cancel modals). The full "Patient Home" dashboard (upcoming-appointment card, recent reports, notification bell, bottom tab bar) and Profile/Settings remain `T-1301` (Phase 13) — this phase deliberately built only what the booking/appointment-management flow needs to be reachable, not the wrapping shell around it.

## Admin status

Authenticated shell live (sidebar/topbar, permission-driven nav, Super-Admin Hospital switcher, working logout) wrapping Branches, Departments, Users, Doctors, Patients, Staff, Doctor Schedules, and (new this phase) Appointments and Calendar screens — plus the public Activate Account page. Root `/` redirects to `/login` when signed out, otherwise a bare "Welcome, {name}" placeholder (the real Dashboard needs `analytics.read`, Phase 11). None of the nine live screens has a separate "Details" route — every entity's fuller per-entity Details screen (docs/09-ADMIN-DESIGN-MOCKUPS.md's tabs for Medical History/Documents etc.) stays deferred until Phase 8 gives those tabs real data; Appointment Details is a modal, matching Doctor Schedules/every other screen's established list+modal convention. Calendar is a simplified single-day view (see Phase 7 Admin section above), not the mockup's full Day/Week/Month with drag-to-reschedule. Everything else in docs/09's 25-screen mockup set (Reports, Notifications, Analytics, ...) doesn't exist yet — the sidebar deliberately only lists what's actually built.

## Testing status

Local quality gate (`pnpm lint && pnpm typecheck && pnpm test`, run via `turbo` with reduced concurrency to fit this sandbox's available memory — see note below) passing across all 12 packages, plus `pnpm test:integration` run separately, **one spec file at a time**, against the pglite stand-in per the "Known issues" caveats (all 6 files green individually; the default multi-file run is unreliable on this host — see "Known issues" for why). Test counts as of this update: `apps/api` 49 unit (up from 32 — `appointment-policy.util.spec.ts`'s 17 DB-free pure-function tests) + 95 integration (16 auth + 10 tenant-management + 8 users + 18 doctors-patients-staff + 15 schedules + 28 appointments, up from 67 — the appointments suite includes the real concurrency race test and the security-review-driven branch-scope regression test), `packages/shared` 15, `packages/validation` 83 (up from 69 — new appointments schema coverage), `packages/database` 5, `packages/ui/tokens` 3, `apps/admin` 54 component tests (up from 45 — Appointments + Calendar screens), `apps/mobile` 42 (up from 18 — 6 new screen spec files). No E2E specs or a dedicated authorization-matrix suite yet (tenant isolation is covered for every domain built so far, including this phase's `appointments.read`/`update`/`cancel`/`checkin` scope split and the reschedule-vs-no-show sub-action gate; a full permission-matrix sweep across every role × every route is a later hardening pass, Phase 12/14).

Note on running the workspace-wide gate in this sandbox: `pnpm quality`/`pnpm lint`/`pnpm typecheck`/`pnpm test` at the root, run through `turbo`'s default full parallelism, can exhaust this sandbox's available memory (observed as V8 OOM crashes in unrelated packages, not a real failure). Re-running with `pnpm exec turbo run <task> --concurrency=2` reliably passes. Likely sandbox-specific, not a real CI concern, but worth knowing if a future session sees a mysteriously OOM-ing `pnpm quality`.

## Deployment status

Not started. Vercel/Render/Supabase topology and manual deployment workflow specified in [32-DEPLOYMENT.md](32-DEPLOYMENT.md); no environments are currently provisioned (Phase 15).

## How to update this file

After any implementation milestone in Stage 2: update "Current phase"/"Current feature," move newly-shipped items into "Completed features," update the five status sections above to reflect what's actually working locally/deployed (not just written), and log any new known issue with enough context (symptom, suspected cause if known, affected task ID) for a future session to pick it up cold.
